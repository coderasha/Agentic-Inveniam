apiVersion: v1
kind: Service
metadata:
  name: gain-identity
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: gain-identity
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
